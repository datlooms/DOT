================================================================================
  equiDOT BOOK-50 — POST-UPDATE VERIFICATION CHECKLIST
  Use after completing the DOT50 development plan (the 3 changes).
  Every item must PASS before demo trading begins.
================================================================================


═══════════════════════════════════════════════════════════════════════════
 PHASE A: COMPILATION & STARTUP
═══════════════════════════════════════════════════════════════════════════
[ ] A.1  Zero-warning compilation. 0 errors, 0 warnings in MetaEditor.
[ ] A.2  EA loads on chart without crash or journal errors.
[ ] A.3  OnInit reads Dots_InitBars = 6900 bars and seeds the rolling buffers;
         EA starts within seconds (no 30K history wait).
[ ] A.4  Panel displays 50 signals across the 8 structures (37L / 13S).
[ ] A.5  Dots externs present and correct:
         [ ] Dots_RollingBufferSize = 2500   (SACRED, unchanged)
         [ ] Dots_InitBars = 6900            (SACRED, unchanged)
         [ ] Dots_LeapFrog_StepMult = 0.30
         [ ] Dots_MaxRisk = 150.0
         [ ] Dots_RiskMult = 2.0
         [ ] Dots_Spread = 3.0
         [ ] Dots_LiveRiskJar = 6 (lots; REPLACES Dots_MaxPositions count cap)
         [ ] Dots_RequireConcurrent = true, Dots_MinConcurrent = 2
         [ ] Dots_SoloVolumeGate = 300
[ ] A.6  Journal confirms rolling buffers seeded from the 6,900 chart bars.
[ ] A.7  No retired threshold mechanism present: no static-hardcoded (A),
         no ATR-scaled (B), no regression-EMA (C) threshold paths. Only
         Mechanism D (rolling-2500 percentile) + structural (VWAP_Z, OR_Position).


═══════════════════════════════════════════════════════════════════════════
 PHASE B: SACRED LAYER UNTOUCHED (the most important phase)
═══════════════════════════════════════════════════════════════════════════
[ ] B.1  Adaptive calibration layer byte-identical to the frozen baseline in
         every respect except the 3 authorized changes.
[ ] B.2  dots_thresholds.py oracle unchanged (sha 518862bf…); all thresholds —
         F0 legs AND the F1 legs (Sqz_Val:hi, Micro_OrderFlowDelta:lo) — route
         through it. No new threshold source was introduced.
[ ] B.3  KAMA warm-start persistence layer unchanged (file write/read/validate/
         anchor; export cold-guard save/restore; signal suppression on cold start).
[ ] B.4  Cold-start correctness fixes intact (HarmVol EMA seed, KAMA anchor-seed,
         PoC day-boundary, RebuildStateForExport δ=0).
[ ] B.5  Export schema unchanged (171/172 columns).
[ ] B.6  ResizeAndSmartShift remains the sole shift authority; one shift per bar;
         sacred calculation pass order preserved; zero terminal overrides.
[ ] B.7  S.7 trade-management constants unchanged (SPREAD 3.0, RISK_MULT 2.0,
         MAX_RISK 150.0, STEP_PCT 0.30; BE = +1 step; tier-3 LeapFrog activate;
         Friday 16:45 close). MAX_POSITIONS=6 is REPLACED by the 6-lot live-risk
         jar (change d) — the SOLE authorized S.7 change; all other S.7 constants stay sacred.


═══════════════════════════════════════════════════════════════════════════
 PHASE C: MOMENTUM-RUNNER TM (change a)
═══════════════════════════════════════════════════════════════════════════
[ ] C.1  Per-position entryLogReturn captured at Dots entry (from the closed
         entry bar's Micro_LogReturn) — causal, no history lookup.
[ ] C.2  leapFrogLag set once at entry: (Micro_LogReturn*dir >= 0.00012) ? 3 : 2.
[ ] C.3  The lag switch is CORRECT: high-velocity entries (v >= 0.00012) get
         lag 3; all others get lag 2 (the S.7 default).
[ ] C.4  The per-position lag is used ONLY in the LeapFrog trail arithmetic
         (SL = entry ± (TiersReached − leapFrogLag) × step), only when
         TiersReached >= 3, and only moves the SL favorably.
[ ] C.5  UNCHANGED by the runner: initial SL min(ATR×2,150), BE nudge (+1 step),
         tier detection, Friday close, entry logic.
[ ] C.6  Toggling the runner off reproduces the jar S.7-base numbers
         (PF 5.66 / net $56,663 / OOS PF 5.96); on reproduces jar RUN
         (PF 5.83 / net $58,685 / OOS PF 6.57). Worst-day −$127.5 in both.


═══════════════════════════════════════════════════════════════════════════
 PHASE D: SEQUENTIAL-LATCH SUBSYSTEM (change b) — the 2 F1 signals
═══════════════════════════════════════════════════════════════════════════
[ ] D.1  Two boolean ring buffers exist (length 16), one per F1 leg-A latch
         (Sqz_Val:hi & ST_Flip ; ADX_Rising==0 & ST_Flip), plus a rolling index.
         They are the ONLY new persistent state.
[ ] D.2  The buffers resize/shift under ResizeAndSmartShift discipline
         (one shift per bar); no separate shift path was introduced.
[ ] D.3  Latch write occurs once per bar, AFTER the bar's variables and
         ST_Flip_Event are final. Sqz_Val:hi uses the SAME adaptive threshold
         as the F0 path.
[ ] D.4  F1-SB fires correctly: latch(Sqz_Val:hi & flip) from 13 bars ago AND
         Micro_OrderFlowDelta:lo now AND D2D_Trend_Dir==+1 -> LONG.
[ ] D.5  F1-TE fires correctly: latch(ADX_Rising==0 & flip) from 8 bars ago AND
         D2D_DirStep==-1 now AND D2D_Trend_Dir==+1 -> LONG.
[ ] D.6  The latch never looks back further than its lag; the lagged index math
         wraps correctly ((idx − k + 16) % 16).
[ ] D.7  ST_Flip is the SHARED anchor (reuses the already-tracked ST_Flip_Event);
         no duplicate flip tracking was added.
[ ] D.8  NO new exit, NO new gate, NO second evaluation loop. Once fired, an F1
         signal is an ordinary Dots entry using the same gate and S.7/runner exit.
[ ] D.9  CAUSALITY: the latch uses leg A + anchor from k bars ago and leg B now —
         no future information. Confirmed against a fresh export (leg timing).
[ ] D.10 Standalone alignment: F1-SB ≈ 33 tr / WR 93.9% / PF 9.69 / 6/6 / streak 1;
         F1-TE ≈ 42 tr / WR 92.9% / PF 4.75 / 6/6 / streak 1.


═══════════════════════════════════════════════════════════════════════════
 PHASE E: THE 50-SIGNAL PANEL (change c)
═══════════════════════════════════════════════════════════════════════════
[ ] E.1  Exactly 50 signals registered: 48 F0 (triggerKind F0_TRIPLE) +
         2 F1 (triggerKind F1_SEQ). 37 LONG / 13 SHORT.
[ ] E.2  Each F0 signal = 3 conditions + direction + D2D gate; thresholds via
         the oracle; matches DOT_rule_master_spec.txt PART I exactly.
[ ] E.3  The 2 F1 signals match PART II exactly (legs, lags, direction, structure).
[ ] E.4  All 8 structures represented on the panel: TREND_CONTINUATION 15,
         MOMENTUM_IGNITION 14, BREAKOUT_EXPANSION 8, STRUCTURAL_ENTRY 6,
         PRICE_ACTION 4, SQUEEZE_BREAKOUT 1, TREND_EXHAUSTION 1, VOLUME_CONFIRMED 1.
[ ] E.5  Entry gate unchanged for all 50: ≥2 concurrent OR solo + Volume ≥ 300;
         eligibility ADX ≥ 15 AND Volume > 50; D2D-direction-gated.
[ ] E.6  Macro #defines (if any new) appear before first use across ALL sections
         (positional preprocessor; cross-section dependency checked).


═══════════════════════════════════════════════════════════════════════════
 PHASE F: LIVE-RISK JAR (change d) — the 6-lot live-risk jar
═══════════════════════════════════════════════════════════════════════════
[ ] F.1  live-lot counter g_dots_live_lots increments (+1) on Dots position open.
[ ] F.2  counter decrements (−1) when a position reaches BREAK-EVEN (BE frees the lot);
         exactly once, guarded on the be flag (no double-decrement).
[ ] F.3  a still-live exit (pre-BE close / SL hit) decrements; a post-BE close does NOT
         (its lot was already freed at the BE transition).
[ ] F.4  a new signal is admitted ONLY when live lots < 6 (room in the jar); a full jar
         (== 6) skips the signal (exactly as the count cap skipped a 7th position).
[ ] F.5  the jar NEVER exceeds 6 live lots across a full backtest run (hard bound).
[ ] F.6  backtest reproduces the jar book (2,409 trades / net $58,685 / PF 5.83 / OOS 6.57),
         +74 trades vs the old count cap — confirming the mechanic matches the model.
[ ] F.7  the OLD MAX_POSITIONS count cap is FULLY removed — no residual count-based blocking.


═══════════════════════════════════════════════════════════════════════════
 PHASE G: PER-BAR EXECUTION MODEL (change e) — per-tick -> per-bar SL
═══════════════════════════════════════════════════════════════════════════
[ ] G.1  the DOTS SL repositions ONLY on a new bar (not per-tick); ManageDotsPositions is
         gated to new-bar detection (call moved inside the isNewBar block, or Time-guarded).
[ ] G.2  arm / tier / trail are computed off the CLOSED bar’s High/Low (High[1]/Low[1]),
         NOT the live Bid/Ask tick and NOT the Close.
[ ] G.3  the BROKER hard SL still executes intrabar on any tick (downside protection is
         ALWAYS live per-tick); only the EA’s REPOSITIONING of the SL is per-bar.
[ ] G.4  per-bar sequence order per open position = exit-check -> tiers -> BE-arm -> trail.
[ ] G.5  EA per-bar management reproduces portfolio_simulation_engine.py trade-for-trade on
         a parity run (same entries, same SL levels, same exits — live == the validated book).


═══════════════════════════════════════════════════════════════════════════
 PHASE H: MOMENTUM-CONDITIONAL INITIAL SL (change f)
═══════════════════════════════════════════════════════════════════════════
[ ] H.1  on entry, if lr×dir (Micro_LogReturn × dir) >= 0.00012 the CATASTROPHE SL uses
         min(ATR×4, 150); non-momentum entries use min(ATR×2, 150). (SL = entry ± catastrophe_risk.)
[ ] H.2  NO trade’s initial risk EVER exceeds 150 at any ATR — the $150 cap (Dots_SL_Cap)
         is inviolate; nothing risks more than $150 + spread.
[ ] H.3  the momentum gate reuses the SAME lr×dir value the runner uses (change a / S.12)
         — NO new variable, NO new buffer.
[ ] H.4  THE SPLIT (S.19, EA-CRITICAL): the BE-arm trigger and step_size use
         base_risk = min(ATR×2, 150) — NOT the widened ATR×4 risk. Only the catastrophe SL
         uses the momentum-widened risk. (Confirm step = Dots_StepFrac × base_risk, not × risk.)
[ ] H.5  RISK_MULT / Dots_SL_Mult = 2.0 stays the base constant (S.17 unaltered).
[ ] H.6  worst-day reproduces −127.5 (NOT −320) and max-DD −165.6 — the survival floor is HELD;
         a worst-day near −320 with SL rising to ~202 means BE/step were wired to the widened
         risk (the split is wrong).
[ ] H.7  backtest reproduces the momentum-SL book (166 SL, net $58,249, WR 92.8%, PF 6.12,
         OOS PF 6.99, min-fold 5.49) confirming the split; runner LF count/net unchanged.


═══════════════════════════════════════════════════════════════════════════
 PHASE I: CONVICTION SELF-SCALING + GAP-SINGLES (change g / S.20)
═══════════════════════════════════════════════════════════════════════════
[ ] I.1  book-LONG entries with Micro_Hurst > adaptive p90 open at 2.0 lots; all other book
         trades (and ALL shorts) open at 1.0 lot — Micro_Hurst sizing is LONGS ONLY.
[ ] I.2  book longs within 5 bars of a Micro_FailedBreak-extreme open at 1.25 lots; a long
         qualifying for both Hurst and recentFB takes the HIGHER (2.0), never the product.
[ ] I.3  the two gap-singles fire ONLY when zero Dots positions are open (any open body,
         live-risk OR breakeven’d, blocks them). Hurst: Micro_Hurst>p97 & D2D==+1 LONG;
         FailedBreak: Micro_FailedBreak>p90 & D2D==-1 (counter) LONG. Gate ADX>=15 & Vol>=300.
[ ] I.4  gap-singles use LOCK=3 and 1.0 lot (NOT conviction-sized).
[ ] I.5  base lot is 1.0 (the multiplier IS the scaling) — NO blanket-2x on top (SACRED: 1-LOT
         BASE ONLY; 2-lot base breaches the FTMO gate at a 12-lot stack).
[ ] I.6  reproduces the G book: 2,691 tr, WR 92.2%, PF 6.15, net $89,487, worst-day -153.7, OOS PF 6.81
         (honest pack-engine figure; the modelled $90,103 carried a research-harness artifact —
         worst-day -153.7 is ~16x inside the -$2,500 gate at 1-lot base).


═══════════════════════════════════════════════════════════════════════════
 PHASE I.S21: D2D CROWN JEWEL (change h / S.21, RATIFIED)
═══════════════════════════════════════════════════════════════════════════
[ ] S21.1 short_mult sizes SHORTS -- book x2 count RISES with Role 2 on (210 -> 249); a
          longs-only build silently drops D2D's short conviction. REQUIRED structural capability.
[ ] S21.2 D2D-gap all exactly 2.0 lots, NO 4x -- lots assigned directly, never routed through
          the conviction arrays (the D2D-agree condition is true on every gap bar).
[ ] S21.3 warm-up guard on ALL 3 gap sources (Hurst, FailedBreak, D2D): min gap entry bar >= 6900.
[ ] S21.4 gap priority: one gap per flat-bar, D2D-gap -> S.20 gaps; D2D-gap uses LOCK_FRAC=1.0,
          S.20 gaps keep GAP_LOCK=3.0 (deliberate asymmetry, not a bug).
[ ] S21.5 reproduces the crown jewel: 2,698 tr / WR 92.3% / PF 6.40 / net $92,347 / daily worst-
          day -104.4 / daily max-DD -145.9 / 6/6 folds (min fold PF 5.39) / OOS PF 6.96 / net $29,326.
[ ] S21.6 oracle (dots_thresholds.py 518862bf), wf.py, core.py BYTE-IDENTICAL.


═══════════════════════════════════════════════════════════════════════════
 PHASE J: STRATEGY TESTER ALIGNMENT
═══════════════════════════════════════════════════════════════════════════
[ ] J.1  RUN book reproduces within tolerance: WR 91.9%, PF 5.83, net $58,685
         @1lot, worst-day −$127.5, max-DD −$165.6.
[ ] J.2  Persistence reproduces: 6/6 folds, 22/22 ISO-weeks, 5/5 weekdays,
         min-fold PF 5.01.
[ ] J.3  March bear fold reproduces: PF 5.47 / net $14,500 (positive through the
         −10% crash).
[ ] J.4  OOS (May–Jun) reproduces: PF 6.57, WR 92.2%, net $18,926.
[ ] J.5  Survival margins hold: worst-day 19× inside −$2,500; max-DD 60× inside
         −$10,000.
[ ] J.6  Both F1 signals contribute trades in the tester run; the runner lag
         switch is observed on high-velocity entries.


═══════════════════════════════════════════════════════════════════════════
 PHASE K: DIRECTIVE RECONCILIATION (A2.7) & PARITY
═══════════════════════════════════════════════════════════════════════════
[ ] K.1  COMPLETENESS: all 8 structures present; all 50 signals live.
[ ] K.2  ADAPTIVITY: every threshold self-computes through the oracle regardless
         of chart history.
[ ] K.3  PARITY: fresh export reproduced bit-for-bit by dots_thresholds.py
         (export = live). No look-ahead anywhere, including the F1 latch. Per-bar SL
         management reproduces portfolio_simulation_engine.py trade-for-trade (parity run).
[ ] K.4  CONSISTENCY: the sacred layer is byte-identical except the 6 authorized
         changes; no retired mechanism returned.
[ ] K.5  The complete system — not just the 6 edits — satisfies all standing
         directives. Any gap flagged and resolved before sign-off.

FINAL: all items PASS -> proceed to the FTMO demo (the true final test).
       Any FAIL -> stop, fix, re-run the affected phase.
