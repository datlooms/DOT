AUDITOR — FIFTH PASS (SCOPED)
STATE

HEAD e0eb79a. DEV_CHECKLIST.md 250 lines / 715a4f3499a9 ✓ · DEVELOPER_BRIEF.md 123 lines / b8c7102652a0 ✓ · master.py 3d45bd3b7f74 and the sacred five unchanged ✓.

Delta confined — verified by measurement, not by your word. An opcode-level region diff against 9fb0e40253ef shows changed regions in exactly three places: ## APPENDIX D, the INVALID row, and the three concurrence_profiler rows. V2 SURVIVAL is byte-identical to the clause I ratified (compared directly, not inferred). Everything else — 24 items, Appendices A and B, the UNEVALUABLE table, signal_id, reason_code, determinism, item 5's pointer — unchanged, not re-verified.

AMENDMENTS 3 AND 4 — BOTH APPLIED CORRECTLY

concurrence_profiler now has three rows: 239 the console depth distribution; 841 np.argsort(-J[iu])[:TOP_PAIRS_N], correctly described as a fixed top-N over a Jaccard matrix emitted to CSV, sanctioned on the ground that a fixed documented N is not a percentile; 1072 np.sort(v) in the circular-shift null, a null-statistic array. The INVALID row now reads "a single signal exceeds the daily ceiling on train" with the not-the-account-test note — the contradiction with V2's "item 16 guards the account. Do not merge them" is closed. ✓

POINT 5 — WRITTEN IN, ACCURATELY

Both stages are now on the page, stage (i) is named as the unstable one, and cluster_profiler.py L132 is given as the exact ambiguous case with its classification stated. That is what I asked for and it is stated more precisely than I put it. ✓

THE FOUR CHALLENGES
1. Is locking three modules sufficient? NO. There is a fourth, and it is the one with prior form.

I ran my own constructor sweep — every definition of build_terrain, build_cluster_set, thrust_events, eligible_universe, atr_buckets, kmeans, *regime_labels, build_category_depth across dot_master_discovery/. It returns three files: terrain.py, cluster_profiler.py, and scanners/concurrence_profiler.py.

concurrence_profiler.py defines market objects today:

compute_regime_labels (L1146) and causal_regime_labels (L1099) assign every bar a regime label — a stratum over market data — via kmeans (L347) / bic_kmeans (L393) over zscored market features. Not mechanism D. Not a percentile either, which is precisely why a token sweep never saw it and why the file-level guard is the right shape.
causal_category_membership (L931) / build_category_depth (L963) define per-condition category membership; align_pool (L244) and depth_arrays (L261) partition the pool and build per-bar depth.

It is F12's module, invoked by the pipeline, and its strata reach emitted artifacts. And it is the module that has already produced exactly this defect class once: a full-sample regime label gating entries, fixed by splitting lab_desc from lab_causal. It is the single most likely site of a recurrence and it sits outside the lock.

It is also free to lock: my independent grep of items 1-24 finds only master.py ×5, family_evidence.py ×2, selection.py ×1, score_book.py ×1. terrain.py, cluster_profiler.py, concurrence_profiler.py and analysis_engine.py appear in no build item — which independently confirms your grep for the two you did lock.

2. Is the residual still narrower than the truth? Yes — in its premise, not its structure.

The carve-outs are right: a new module escapes, and so does a definition written into master.py / selection.py / family_evidence.py, which cannot be locked because this build edits them. You widened that correctly and the "not mechanical closure over the whole codebase" statement is honest.

But the residual rests on the claim that file-locking "secures the market-object surface that exists today, in three named modules". The surface that exists today is four modules. So the residual understates the gap not by omitting a category but by mis-enumerating the one category it claims to have closed. You were right to suspect the width — it is still short, and this time on the enumeration rather than the edited-files dimension.

3. Is sha-drift the same guard as the sacred five, or only in form? Only in form, as written.

The sacred five carry their shas in the document — 518862bf19fb, 793e6e5f8d9a, and so on. Anyone can detect drift against a pre-agreed constant, and the Developer cannot move the baseline.

terrain.py and cluster_profiler.py say "(record sha at build time)". The baseline then becomes whatever the Developer's working tree contains at the moment of recording. If either file were edited before recording — deliberately, or by an IDE reformat, or by a stray line-ending change — the lock certifies the edited file and reports "no drift" for the rest of the project's life. That is self-certification. It has the form of the sacred-five guard and not its substance, and the failure is silent, which is the property this whole appendix exists to eliminate.

The fix is one line each, and I have computed the values from HEAD so it is actionable immediately:

engine/terrain.py                dcaecaf7e8e1
engine/cluster_profiler.py       070bb2aa7aaa
scanners/concurrence_profiler.py 554019e93069
4. Does removing the line-keyed manifest lose coverage that mattered? A little, and it is recoverable for free.

Accounting: the manifest held 34 sites across 8 files. File-locking now covers 10 — terrain.py 3, cluster_profiler.py 7 — plus dots_thresholds.py, whose own idiom (builtin sorted) the sweep never caught, which is itself the argument for the file-level form. 24 sites are unguarded.

Of those, 20 sit in selection.py, master.py and family_evidence.py — edited by the build, unlockable, correctly covered by your residual. The remaining 4 sit in analysis_engine.py (1) and concurrence_profiler.py (3), neither of which any build item touches. So the loss is real but small, and it is fully recoverable at zero cost on the module that matters. analysis_engine.py's single site is stable exit-bar ordering over a book-statistics module — locking it is optional and I would not press for it.

Removing the line-keyed manifest was right. It was going to fire on the first mandated commit — I verified split_tree() is still at master.py L111-122 and that 20 of 34 sites sit in the three files the checklist orders edited. Trading a guard that self-destructs for one that is idiom-blind is the correct trade, and taking file-locking over index-narrowing was the better of the two routes I offered: index-narrowing would still have been a pattern match on idiom, which had just failed twice.

VERDICT: RATIFY WITH AMENDMENTS

Two amendments. Neither blocks the build — Appendix D is a pre-run guard, and the build path is unaffected by both.

Add scanners/concurrence_profiler.py to the byte-locked market-object modules, and correct the residual's enumeration from three modules to four. It defines regime strata over market data by k-means, it is invoked by the pipeline, no build item edits it, and it has already produced a look-ahead-by-stratum defect once. Locking it costs nothing and closes the one gap in the surface the appendix claims to have secured.
Record the lock shas in the document now rather than "at build time". terrain.py dcaecaf7e8e1, cluster_profiler.py 070bb2aa7aaa, concurrence_profiler.py 554019e93069 — all computed from HEAD e0eb79a. A baseline the Developer records himself is self-certification; a baseline stated in the document is the same guard the sacred five actually are.

Both are single-line changes and both are verifiable on sight.

The mechanism is otherwise sound and the reasoning behind it is right. File-locking closes by construction what enumeration kept failing to close, the two-stage test is now stated where it is applied, the classification record is correctly demoted from enforcement to documentation, and the residual — once its enumeration is fixed — is the honest statement of what is and is not mechanically closed. Everything else in both documents remains as ratified.

I formed no expectation about this pass and I am forming none about the next.

Reporting and holding.