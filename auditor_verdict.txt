AUDITOR — SEVENTH PASS (SCOPED)
STATE

HEAD 30093d6. DEV_CHECKLIST.md 265 lines / 1f9d770f3b2c ✓ · DEVELOPER_BRIEF.md 123 lines / 178c77f52d18 ✓ · master.py and the sacred five unchanged ✓. All three lock shas still current — terrain.py dcaecaf7e8e1, cluster_profiler.py 070bb2aa7aaa, concurrence_profiler.py 554019e93069 — verified against this clone, not carried from your report.

Delta confined — measured. Region diff against d787d586627b returns one changed section, ## APPENDIX D; Appendices A, B and C byte-identical as whole blocks, which covers V2, the UNEVALUABLE table and the two-stage test without inference. Items 1-24, lock table, classification record — unchanged, not re-verified.

CHALLENGE 3 — DID THE TERRAIN EVIDENCE DEFEAT MY WORDING? Yes. You read it correctly and I was wrong.

Verified in source: GRID_W = (15, 30) is a forward-window length consumed per bar at thrust_thresholds(df, W, …) (L107-108); CONTIGUOUS_TOLERANCE = 1 groups events within one bar at build_cluster_set (L114). Neither requires knowing the population to place an element. My wording — documented constants over market data should still be prohibited — would have forbidden the ratified terrain construction outright. Substituting a measured discriminator for my formulation was the right call, and this is the second time you have overruled me on measurement and been right.

Note in passing that the new rule handles TOP_EPISODE_FRACTION = 0.10 (L57) correctly: it is population-dependent, and it is already the flagged terrain.py L177 defect. The rule is self-consistent there.

CHALLENGE 4 — BOTH RE-BASED SANCTIONS HOLD

concurrence_profiler L841: population-dependent (the ranking needs every pair), so the fixed-N defence correctly does not survive — and it is sanctioned on stage (ii) alone because the objects partitioned are condition pairs, not bars, episodes or strata. family_evidence L280: each size maps to a literal band independently of every other row, so not population-dependent, and cluster size is a book quantity — holds on both stages. Re-basing them rather than leaving the old ground in place was necessary, and it was done.

CHALLENGE 2 — N / total = p IS IMPRECISE, BUT THE CONCLUSION IS RIGHT

For a fixed N and a varying total, N / total is not a fixed p — the identity holds only for a given total, so "a fixed top-N is a percentile with the parameter written as a count" is not strictly true. It doesn't open a gap, because the operative reason is the one you also give: to take the top 100 you must first rank all of them. Rest the prohibition on the ranking requirement and the arithmetic claim becomes unnecessary.

And there is a top-N over market data that is genuinely not a percentile: a prefix — "the first N bars". Placing element i needs only i. Not population-dependent, correctly permitted. A suffix — "the last N bars" — needs the total, and is correctly prohibited. That asymmetry is handled cleanly by dependence and would have been ambiguous under "is it a percentile?", which is a point in the new rule's favour.

CHALLENGE 1 — DOES POPULATION DEPENDENCE DRAW THE LINE IN THE RIGHT PLACE?

It is the right axis, and it over-catches in exactly the symmetric way mine did. Two findings.

(a) The rule, read literally, prohibits the ratified walk-forward and fold construction. Verified in source:

master.py L337-339: n_oos = int(round(n * OOS_TAIL_FRACTION)), train_days = days[:n - n_oos], oos_days = days[n - n_oos:], and base = m // FOLD_COUNT at L341. Placing a trading day into train versus OOS, or into fold i, requires knowing n — the total post-warmup trading days.
wf_selection.py L131-140: remaining = total_days - first_valid, base = remaining // n, extra = remaining % n — the split derivation, same shape.

Both are partitions of market data (trading days), both are population-dependent, both sit outside dots_thresholds, and both are ratified design — I ratified the proportional folds and the split derivation in earlier passes. By the scope sentence's own words — "an episode, cluster, stratum or any other partition of market data" — they are in scope and prohibited. That is the mirror of the GRID_W problem you used to defeat my wording.

The fix is small and preserves everything the rule was built to catch: narrow "the rest of the population" to "the distribution of a market quantity across the population." A fold split depends only on the cardinality and ordering of the time axis — no market value is ranked or thresholded. TOP_EPISODE_FRACTION, top-N by displacement, and every percentile still bite, because they depend on the distribution of a market quantity. GRID_W and CONTIGUOUS_TOLERANCE remain permitted. The fold and split constructions become permitted, correctly.

(b) One contamination class the new rule cannot see, and my rule could. A constant whose value was derived from this span offline and then hardcoded — someone computes p90 of displacement, gets 42.7, writes disp > 42.7. At runtime each element is classified independently, so it is not population-dependent and the rule permits it. But it encodes the full-span distribution, which is precisely the look-ahead contamination the scope sentence names. My over-broad wording caught this; the new one does not. Neither is clean, because this is not detectable from form at all — it needs provenance, not shape. The appendix has a residual section, and this belongs in it: a constant applied to market data should state where its value came from — a design choice, or a number read off this data. Nothing in the document currently distinguishes TOP_EPISODE_FRACTION = 0.10 chosen by design from the same figure chosen because it split this span nicely.

THE pd.cut STAMP AND OWNERSHIP — CORRECT

Re-run rather than re-cited, stamped at HEAD 39fbd4d, and — the better half — assigned: "re-running this sweep is part of the lock-manifest review, and that review owns it." You were right that dating alone tells a reader the claim is stale without telling anyone to act. That is the stronger form of what I asked for.

THE core.py CLAUSE — STRONG ENOUGH, WITH ONE GAP IN PLACEMENT

It names the trigger (EA fix and re-export, live), the consequence (the enumeration silently reads four while five exist), and the action (it joins the lock table). Bolded and headed "Dependency, not a footnote." That survives a reader who is not looking for it.

The gap is where it lives. Anyone removing core.py from sacred status will edit the sacred-five line at the top of this document — line 3 — and has no reason to open Appendix D. Put a one-clause pointer on the sacred-five line itself: that core.py and dots_thresholds.py are also Appendix D market-object definers and cannot be un-sacred'd without joining that lock table. Then the trigger is stated where the change is actually made.

VERDICT: RATIFY WITH AMENDMENTS

None of the amendments blocks. The build may start; the brief may go to the Developer tonight. Every finding below is in a review criterion inside Appendix D, not in the build path: the enforcement mechanism is the four-module file lock, and neither master.py nor wf_selection.py is locked, so the over-catch I found misleads a future reviewer rather than aborting a run. Nothing here touches the catalogue, any figure, or any item the Developer builds from.

Narrow the discriminator to the distribution of a market quantity. As written it prohibits the ratified proportional fold/OOS split (master.py L337-341) and the walk-forward split derivation (wf_selection.py L131-140), both population-dependent partitions of trading days. Depending on the cardinality of the time axis is not depending on the distribution of a market quantity, and only the latter is contamination.
Add the fitted-constant class to the residual. Population dependence is a runtime-form test and cannot see a constant whose value was read off this span. Require constants applied to market data to state their provenance — this is a review obligation, not a mechanical one, which is where the residual already puts such things.
Rest the top-N prohibition on the ranking requirement, not on N / total = p, which holds only for a fixed total.
Move the core.py trigger to the sacred-five line (or cross-reference it there), because that is the line someone edits when un-sacreding it.

On your stated worry — whether population dependence is the complete axis: it is the right axis and it is not complete, but the class it misses is one no form-based rule can catch, and the answer is provenance rather than a third discriminator. That is a boundary of the method, not a fault in your formulation. You have now substituted your own reasoning for mine twice and been right both times; this pass I am refining your rule rather than replacing it, which is the correct direction of travel.

I formed no expectation coming in. The work is sound and the guard does what it claims.

Reporting and holding.