AUDITOR — THIRD PASS (SCOPED)
STATE

HEAD 3143f5f. DEV_CHECKLIST.md 210 lines / c85d3e4be524 ✓ · DEVELOPER_BRIEF.md 123 lines / e29a28ea1441 ✓. master.py 3d45bd3b7f74 and the sacred five unchanged ✓. Delta against what I audited: +27/−6 and +3/−3 — consistent with the five scoped items and nothing else.

Out of scope, unchanged, not re-verified: all 24 items' line numbers and function names, the indivisible block, item 15's branch, Appendix A and B fidelity, every load-bearing figure, the ladder, the F0-vs-MIXED penalty, A5's reasoning. I confirmed the delta touches none of them.

1. APPENDIX C, POST-FIX — both blockers closed; one new defect

B1 applied exactly as specified. UNEVALUABLE := **V1, V3 or V4 unmet**, with V1 named as the highest-volume rejection path that "MUST route here, not fall through undefined". The reason_code line is now annotated 1:1 with its clauses — insufficient_trades (V1), pf_undefined (V3a), insufficient_active_days (V3b), insufficient_buckets_direction (V4) — which is better than I asked for: the mapping is now auditable on the page rather than reconstructable. INVALID count per family added with the argument stated. ✓

B6 applied and verified in code. signal_id is family|signal_definition|direction, pipe-delimited. I checked the parser: master.py L1194 is fam, sig, dr = key.split('|', 2) — exactly three parts in exactly that order. ✓

NEW DEFECT — V2's evaluation basis is ambiguous, and V2 is the only clause that drops rows.

V2 now reads: "worst_day(s,T) >= -FTMO_DAILY_CEILING… Evaluated on the FULL population including gap fillers, because the ceiling does not distinguish sources."

worst_day(s,T) is a per-signal quantity — s is the subject of the predicate. But "FULL population including gap fillers" is book-level wording, and it is inherited verbatim from Appendix B's absolute-survival row ("worst modelled day against the FTMO ceiling, FULL population"), which I verified in pass one as faithful to spec §6.2. Gap fillers are GAP_HURST / GAP_FB / GAP_D2D — synthetic entries attributable to no book signal (verified). A per-signal worst day therefore cannot include them.

Two readings, two different tests: (a) per-signal, min over days of that signal's own trades — which is what your measurement computed; (b) book-level, min over days of all trades — identical for every s, so V2 admits all or none. The clause supports both. Since V2 is the sole route to INVALID, and INVALID rows leave the catalogue entirely, an ambiguous basis on a drop rule is worth closing. It cannot bite on this data (both bases sit far inside −2500), so this is a specification defect, not a run risk.

2. APPENDIX D — sweep verified complete; the mechanical form is evadable

The sweep is complete. I ran my own, independently, across every .py in dot_master_discovery/. 16 occurrences of np.percentile / np.quantile / .quantile(: 1 comment (concurrence_profiler.py L237 — confirmed a comment, exactly as you say), 14 SANCTIONED, 1 FLAGGED (terrain.py L177). Zero sites outside the allowlist. Every line number in the appendix table is correct, including the three cluster_profiler sites you had missed. No further misses.

But the mechanical form is evadable, and the codebase already contains the evasion. You asked whether the allowlist makes it mechanical or only appears to. It is mechanical for the two named function forms and blind to everything else:

concurrence_profiler._depth_quantiles computes a percentile by np.sort(v) plus an index. Its own comment states the position outright: "Sorted-index, not np.percentile, so the file contains no percentile call anywhere." A grep-based allowlist on those two names cannot see it.
selection.py L229-232 does the same shape: np.sort(ev)[::-1] then np.searchsorted(cum, 0.90) / 0.95 — a 90/95% cumulative cut computed without either function name.

Both are harmless today (console and descriptive respectively). But they prove the hole: a Developer defining a market stratum by sorted index would never trip the abort, which is precisely the failure the appendix exists to prevent — and the one instance you did catch, terrain.py L177, only tripped because it happened to use np.percentile.

The scoping rule itself holds up. "Market-object definition, not book-statistic estimator" is stable enough to apply consistently, because it reduces to an operational test a Developer can run: does the percentile partition market data — bars, episodes, strata — or does it summarise the P&L of an already-chosen set? terrain L177 partitions episodes → market object. selection L348's τ over a signal's own daily P&L summarises a chosen signal's outcomes → book statistic. That test resolves every site in the table.

One case is genuinely hard, and it is the one shape the prohibition targets: selection.py L725, keep = obs >= np.quantile(obs, drop_quantile) — a quantile that defines a kept set. It is correctly sanctioned (it sits in the P3-parked stability-selection machinery and partitions statistics, not market data), but it is lumped into a single row with L711 and L740 under "null critical value, kept-set quantile". Three sites, two descriptions, and the hardest call left implicit. Give L725 its own line and its own one-clause rationale, or a later reader will re-litigate it.

3. V2 / B2 — MEASUREMENT VERIFIED. CLOSING THE ITEM. DELETION WAS RIGHT.

I reproduced it independently on the corrected frame:

quantity	your figure	mine
worst single signal's worst day	−$204.7	−$204.7 ✓
signals failing V2 at MARGIN 0.0 / 0.1 / 0.2 / 0.5	0 / 0 / 0 / 0	0 / 0 / 0 / 0 ✓
median signal worst day	−$142.6	−$145.0
book-level worst day	−$639.1	−$639.1 on the BOOK-only basis; −$565.3 full-population

The load-bearing claim — V2 is inert as a per-signal test at every margin in a sane range — reproduces exactly. The segment argument is sound: a segment's day set is a subset of the full span's, so its minimum cannot be lower; passing on the full span implies passing on every split. V2 cannot bind inside a walk-forward split.

The decision is correct and I endorse it over my own amendment. My objection was that an undefined parameter makes V2 unimplementable — deletion resolves that as completely as defining would. And deletion is the better resolution on doctrine: with every value in the sane range behaviourally identical, any chosen MARGIN would be an arbitrary constant with no measured basis sitting inside the admission predicate — a tunable knob, which rule 3 exists to keep out. "Lower the ceiling — one number, not two" is the right principle. Retaining V2 as a guard against a pathological single signal, with the binding survival test at book level in item 16, is the correct division. Item closed.

Two figure notes, neither load-bearing. My median is −$145.0 (per-signal worst day = min over exit-days of that signal's daily P&L sum; median of 50; sorted 25th/26th are −146.4 / −143.7). The −$142.6 in the document must come from a slightly different construction — a $2.4 difference on a descriptive figure inside a justification whose conclusion reproduces exactly. And −$639.1 is the non-gap BOOK-only basis; the full-population basis V2 itself names gives −$565.3. That figure is not quoted in the document, so it is not a document defect — but if it ever migrates in, it must carry its basis, which is the same standing construction that produced amendment A4.

4. ITEM 5'S POINTER — PASS

"the local-percentile ABORT is defined in APPENDIX D — it is scoped to MARKET-OBJECT DEFINITION and must not fire on the book-statistic estimators this document itself mandates." Resolves the item-5/Appendix-B contradiction I raised, points correctly, and states the scope inline so a reader who never opens Appendix D still cannot mis-apply it. ✓

5. DETERMINISM SYMMETRY — PASS

"EVERY artifact except the two named below is byte-identical across runs and worker counts and contains no wall-clock — CSV, markdown or otherwise. The run log and book_scored.jsonl are ATTESTATION RECORDS, not artifacts: both are exempt, and both are REQUIRED to carry wall-clock. Nothing else is exempt in either direction."

Both asymmetries closed — the obligation now covers non-CSV outputs explicitly, and "in either direction" forecloses the reverse reading. ✓

VERDICT: RATIFY WITH AMENDMENTS

Two amendments, both precise, neither blocking. The build can start on these documents; both amendments improve a guard or close an ambiguity that cannot bite on the current data.

Name V2's basis. State that worst_day(s,T) is per-signal, computed on that signal's own trades, and that the book-level full-population survival test is item 16's. The "FULL population including gap fillers" phrase is Appendix B's book-level wording sitting in a per-signal predicate, and gap fillers belong to no signal. V2 is the only clause that drops rows, so its basis should not be inferable two ways.
State Appendix D's residual, or close it. The allowlist is mechanical for np.percentile / np.quantile / .quantile( and blind to sorted-index percentiles — and the codebase contains two live sorted-index cuts (concurrence_profiler._depth_quantiles, whose comment advertises the blind spot; selection.py L229-232). Either extend the sweep pattern to np.sort(...)[, argsort and searchsorted, or state plainly that the abort covers the two named forms and that sorted-index cuts are caught by review, not by the check. Also give selection.py L725 its own row and rationale — it is the one sanctioned site whose shape ("defines a kept set") is the shape the prohibition targets.

On the two things you flagged as most likely wrong: the sweep is now complete — I verified it independently and found nothing you missed. The UNEVALUABLE table, which you have edited twice, is correct: V1/V3/V4 route there, V2 routes to INVALID, the four codes map 1:1 onto V1/V3a/V3b/V4 with none missing and none unreachable, and the INVALID count closes the visibility gap. The scoping rule is sound; it is the mechanism enforcing it that has the hole, not the rule.

You decided B2 on measurement against my amendment, and the measurement was right. That was the correct call and the reasoning is now better than what I specified.

Reporting and holding.